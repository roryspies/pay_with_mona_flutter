import 'package:flutter/material.dart';

class ReviewAndPayView extends StatelessWidget {
  const ReviewAndPayView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
